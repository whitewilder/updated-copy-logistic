

import requests
import json
import re
import os
import time

class LargeCodeCleaner:
    def __init__(self):
        self.token = None
        self.auth_url = 'https://your-auth-url.com/token'
        self.api_endpoint = 'https://api-test-dev.tpk.cloud.us.comp/dev'
        self.credentials = {
            "staff_id": 'xxxx',
            "password": 'xxx'
        }
        self.chunk_size = 80
        self.max_retries = 3
    
    def get_token(self):
        if not self.token:
            print("Getting access token...")
            response = requests.post(self.auth_url, json=self.credentials)
            self.token = json.loads(response.text)
        return self.token
    
    def call_api(self, prompt, code):
        token = self.get_token()
        
        data = {
            'messages': [{
                'content': f'{prompt}:\n\n{code}',
                'role': 'developer'
            }],
            'stream': False,
            'context': {
                'overrides': {
                    'approach': None,
                    'use_gpt4v': True,
                    'gpt4v_input': 'textAndImages',
                    'conversational_chain': True,
                    'rag_chain': False,
                    'session_id': 'cleaner-session-001',
                    'uid': '333.3.211.7',
                    'product': 'dev',
                    'model': {
                        'key': "{'azure_endpoint': 'https://abc-multi-gfopenai-us-dev-01.openai.azure.com/', 'model': 'gpt-4-0125-Preview'}",
                        'text': 'gpt-4-0125-Preview'
                    }
                }
            },
            'session_state': None
        }
        
        header = {"Authorization": "Bearer " + token['access_token']}
        
        for attempt in range(self.max_retries):
            try:
                result = requests.post(self.api_endpoint, json=data, headers=header, timeout=120)
                out = result.json()
                response = out['choices'][0]['message']['content']
                return self.extract_code(response)
            except Exception as e:
                print(f"  Attempt {attempt + 1} failed: {str(e)}")
                if attempt < self.max_retries - 1:
                    time.sleep(2)
                else:
                    print("  Returning original chunk due to API failure")
                    return code
        
        return code
    
    def extract_code(self, response):
        patterns = [
            r'```python\s*(.*?)```',
            r'```\s*(.*?)```',
        ]
        
        for pattern in patterns:
            matches = re.findall(pattern, response, re.DOTALL)
            if matches:
                return matches[0].strip()
        
        return response.strip()
    
    def get_line_indent(self, line):
        return len(line) - len(line.lstrip())
    
    def is_block_start(self, line):
        stripped = line.strip()
        if not stripped:
            return False
        
        block_keywords = [
            'def ', 'class ', 'async def ',
            'if ', 'elif ', 'else:',
            'for ', 'while ',
            'try:', 'except', 'finally:',
            'with ',
            '@'
        ]
        
        for keyword in block_keywords:
            if stripped.startswith(keyword):
                return True
        
        return False
    
    def is_top_level_block(self, line):
        stripped = line.strip()
        if not stripped:
            return False
        
        indent = self.get_line_indent(line)
        
        if indent == 0:
            if stripped.startswith('def ') or stripped.startswith('class ') or stripped.startswith('async def ') or stripped.startswith('@'):
                return True
        
        return False
    
    def find_block_end(self, lines, start_idx):
        if start_idx >= len(lines):
            return start_idx
        
        start_line = lines[start_idx]
        start_indent = self.get_line_indent(start_line)
        
        if start_line.strip().startswith('@'):
            decorator_end = start_idx
            for i in range(start_idx + 1, len(lines)):
                line = lines[i]
                if line.strip() and not line.strip().startswith('@'):
                    if line.strip().startswith('def ') or line.strip().startswith('class ') or line.strip().startswith('async def '):
                        return self.find_block_end(lines, i)
                    break
                decorator_end = i
            return decorator_end + 1
        
        for i in range(start_idx + 1, len(lines)):
            line = lines[i]
            
            if not line.strip():
                continue
            
            if line.strip().startswith('#'):
                continue
            
            current_indent = self.get_line_indent(line)
            
            if current_indent <= start_indent:
                if self.is_top_level_block(line) or (current_indent == start_indent and self.is_block_start(line)):
                    return i
        
        return len(lines)
    
    def smart_chunk(self, code):
        lines = code.split('\n')
        total_lines = len(lines)
        chunks = []
        
        import_lines = []
        code_start_idx = 0
        
        for i, line in enumerate(lines):
            stripped = line.strip()
            if stripped.startswith('import ') or stripped.startswith('from '):
                import_lines.append(line)
                code_start_idx = i + 1
            elif stripped and not stripped.startswith('#'):
                break
            elif stripped.startswith('#') and i < 10:
                import_lines.append(line)
                code_start_idx = i + 1
        
        if import_lines:
            chunks.append({
                'type': 'imports',
                'code': '\n'.join(import_lines),
                'start': 0,
                'end': code_start_idx
            })
        
        i = code_start_idx
        
        while i < total_lines:
            line = lines[i]
            
            if not line.strip():
                i += 1
                continue
            
            if self.is_top_level_block(line):
                block_end = self.find_block_end(lines, i)
                block_lines = lines[i:block_end]
                block_code = '\n'.join(block_lines)
                
                if len(block_lines) > self.chunk_size:
                    sub_chunks = self.chunk_large_block(block_lines, i)
                    chunks.extend(sub_chunks)
                else:
                    block_type = 'class' if line.strip().startswith('class ') else 'function'
                    chunks.append({
                        'type': block_type,
                        'code': block_code,
                        'start': i,
                        'end': block_end
                    })
                
                i = block_end
            else:
                standalone_start = i
                standalone_lines = []
                
                while i < total_lines and not self.is_top_level_block(lines[i]):
                    standalone_lines.append(lines[i])
                    i += 1
                    
                    if len(standalone_lines) >= self.chunk_size:
                        break
                
                if standalone_lines:
                    chunks.append({
                        'type': 'standalone',
                        'code': '\n'.join(standalone_lines),
                        'start': standalone_start,
                        'end': i
                    })
        
        return chunks
    
    def chunk_large_block(self, block_lines, global_start):
        chunks = []
        
        header_lines = []
        body_start = 0
        
        for i, line in enumerate(block_lines):
            header_lines.append(line)
            if line.rstrip().endswith(':') and not line.strip().startswith('@'):
                body_start = i + 1
                break
        
        if body_start == 0:
            body_start = 1
        
        body_lines = block_lines[body_start:]
        header_code = '\n'.join(header_lines)
        
        if not body_lines:
            return [{
                'type': 'block',
                'code': '\n'.join(block_lines),
                'start': global_start,
                'end': global_start + len(block_lines)
            }]
        
        base_indent = self.get_line_indent(body_lines[0]) if body_lines[0].strip() else 4
        
        current_chunk_lines = []
        method_start = 0
        i = 0
        
        while i < len(body_lines):
            line = body_lines[i]
            
            is_method_start = False
            if line.strip():
                line_indent = self.get_line_indent(line)
                if line_indent == base_indent and (line.strip().startswith('def ') or line.strip().startswith('async def ') or line.strip().startswith('@')):
                    is_method_start = True
            
            if is_method_start and current_chunk_lines:
                chunks.append({
                    'type': 'class_part',
                    'code': header_code + '\n' + '\n'.join(current_chunk_lines),
                    'start': global_start,
                    'end': global_start + body_start + i,
                    'header': header_code
                })
                current_chunk_lines = []
            
            current_chunk_lines.append(line)
            
            if len(current_chunk_lines) >= self.chunk_size:
                next_safe = i + 1
                for j in range(i + 1, min(i + 20, len(body_lines))):
                    check_line = body_lines[j]
                    if check_line.strip():
                        check_indent = self.get_line_indent(check_line)
                        if check_indent == base_indent and self.is_block_start(check_line):
                            next_safe = j
                            break
                
                for k in range(i + 1, next_safe):
                    if k < len(body_lines):
                        current_chunk_lines.append(body_lines[k])
                
                chunks.append({
                    'type': 'class_part',
                    'code': header_code + '\n' + '\n'.join(current_chunk_lines),
                    'start': global_start,
                    'end': global_start + body_start + next_safe,
                    'header': header_code
                })
                current_chunk_lines = []
                i = next_safe
                continue
            
            i += 1
        
        if current_chunk_lines:
            chunks.append({
                'type': 'class_part',
                'code': header_code + '\n' + '\n'.join(current_chunk_lines),
                'start': global_start,
                'end': global_start + len(block_lines),
                'header': header_code
            })
        
        return chunks
    
    def clean_chunk(self, chunk_code, chunk_type):
        prompt = """RESPOND WITH ONLY CLEAN EXECUTABLE PYTHON CODE. NO EXPLANATIONS OR MARKDOWN.

Clean and improve this Python code:
1. Add clear docstrings to functions and classes
2. Add brief inline comments for complex logic only
3. Rename unclear variables to meaningful names (e.g., x -> counter, df -> dataframe)
4. Fix formatting and follow PEP8
5. Keep all functionality exactly the same
6. Preserve the exact structure (class/function definitions)

Return ONLY the cleaned Python code, nothing else"""

        cleaned = self.call_api(prompt, chunk_code)
        return cleaned
    
    def merge_class_parts(self, chunks):
        merged = []
        class_parts = {}
        
        for chunk in chunks:
            if chunk['type'] == 'class_part':
                header = chunk.get('header', '')
                if header not in class_parts:
                    class_parts[header] = []
                class_parts[header].append(chunk)
            else:
                merged.append(chunk)
        
        for header, parts in class_parts.items():
            parts.sort(key=lambda x: x['start'])
            
            all_code = []
            header_added = False
            
            for part in parts:
                code = part['code']
                
                if header and code.startswith(header):
                    if not header_added:
                        all_code.append(code)
                        header_added = True
                    else:
                        remaining = code[len(header):].strip('\n')
                        if remaining:
                            all_code.append(remaining)
                else:
                    all_code.append(code)
            
            merged.append({
                'type': 'class',
                'code': '\n'.join(all_code),
                'start': parts[0]['start'],
                'end': parts[-1]['end']
            })
        
        merged.sort(key=lambda x: x['start'])
        return merged
    
    def deduplicate_imports(self, code):
        lines = code.split('\n')
        import_lines = []
        other_lines = []
        seen_imports = set()
        
        for line in lines:
            stripped = line.strip()
            if stripped.startswith('import ') or stripped.startswith('from '):
                if stripped not in seen_imports:
                    seen_imports.add(stripped)
                    import_lines.append(line)
            else:
                other_lines.append(line)
        
        import_lines.sort(key=lambda x: (
            0 if x.strip().startswith('import ') else 1,
            x.strip().lower()
        ))
        
        result_lines = import_lines
        if import_lines and other_lines:
            first_non_empty = next((i for i, l in enumerate(other_lines) if l.strip()), 0)
            if first_non_empty == 0:
                result_lines.append('')
                result_lines.append('')
        
        result_lines.extend(other_lines)
        
        return '\n'.join(result_lines)
    
    def process_file(self, input_file, output_file):
        print(f"Reading file: {input_file}")
        
        with open(input_file, 'r', encoding='utf-8') as f:
            original_code = f.read()
        
        total_lines = len(original_code.split('\n'))
        print(f"Total lines: {total_lines}")
        
        print("Analyzing code structure...")
        chunks = self.smart_chunk(original_code)
        print(f"Created {len(chunks)} chunks")
        
        processed_chunks = []
        
        for idx, chunk in enumerate(chunks):
            chunk_lines = len(chunk['code'].split('\n'))
            print(f"Processing chunk {idx + 1}/{len(chunks)} ({chunk['type']}, {chunk_lines} lines)...")
            
            if chunk['type'] == 'imports':
                cleaned = self.clean_chunk(chunk['code'], chunk['type'])
            else:
                cleaned = self.clean_chunk(chunk['code'], chunk['type'])
            
            processed_chunks.append({
                'type': chunk['type'],
                'code': cleaned,
                'start': chunk['start'],
                'end': chunk['end'],
                'header': chunk.get('header', '')
            })
            
            time.sleep(0.5)
        
        print("Merging processed chunks...")
        merged_chunks = self.merge_class_parts(processed_chunks)
        
        final_code_parts = []
        for chunk in merged_chunks:
            final_code_parts.append(chunk['code'])
        
        final_code = '\n\n'.join(final_code_parts)
        
        print("Deduplicating imports...")
        final_code = self.deduplicate_imports(final_code)
        
        final_code = re.sub(r'\n{4,}', '\n\n\n', final_code)
        
        print(f"Writing cleaned code to: {output_file}")
        with open(output_file, 'w', encoding='utf-8') as f:
            f.write(final_code)
        
        final_lines = len(final_code.split('\n'))
        print(f"Done! Original: {total_lines} lines, Cleaned: {final_lines} lines")
        
        return final_code


def clean_code_file(input_path, output_path):
    cleaner = LargeCodeCleaner()
    return cleaner.process_file(input_path, output_path)


if __name__ == "__main__":
    import sys
    
    if len(sys.argv) >= 3:
        input_file = sys.argv[1]
        output_file = sys.argv[2]
    else:
        input_file = "messy_code.py"
        output_file = "clean_code.py"
    
    if not os.path.exists(input_file):
        print(f"Error: Input file '{input_file}' not found")
        sys.exit(1)
    
    clean_code_file(input_file, output_file)